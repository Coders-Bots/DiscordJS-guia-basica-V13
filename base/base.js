const { Client, CommandInteraction } = require("discord.js");
const Discord = require('discord.js');

module.exports = {
    name: "",
    description: "",
    userperm: [""],
    botperm: [""],
    owneronly: false,
   
    run: async (client, interaction) => {
      
    },
};

//Normales

const Discord = require('discord.js');
const config = require("../../config.json")

module.exports = {
    name: "",
    aliases: [""],
    description: "",
    onlycreator: false,
    args: false,
    usage: "",
    category: "",
    permissions: "",
    cooldonw: 3,
    permissionsme: "",
    guildOnly: false,

  async execute(message, args, prefix, client, version) {

  }
}























// pterodactyl.relationships.servers.data[i].attributes.name.slice(0, 12) + "..." : pterodactyl.relationships.servers.data[i].attributes.name

